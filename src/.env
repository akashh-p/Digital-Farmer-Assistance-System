# ── Server ──────────────────────────
PORT=3000

# ── MySQL ────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=farmer
DB_PASSWORD=Farmer@123
DB_NAME=farmer_db

# ── JWT ──────────────────────────────
JWT_SECRET=farmerassist_super_secret_2026
JWT_EXPIRES_IN=7d

# ── Weather API (OpenWeatherMap) ─────
# Free key from https://openweathermap.org/api
# Leave as DEMO to use mock fallback data
WEATHER_API_KEY=DEMO

# ── Market Prices ────────────────────
# We use DB-seeded data + manual admin updates
# (No paid agri API needed for project submission)
